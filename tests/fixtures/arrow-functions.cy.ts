describe("Arrow Function Suite", () => {
  it("should use arrow functions", () => {
    expect(true).toBe(true);
  });

  describe("Nested Arrow Suite", () => {
    it("should handle nested arrow functions", () => {
      expect(1).toBe(1);
    });
  });
});